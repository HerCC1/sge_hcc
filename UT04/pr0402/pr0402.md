# Ejercicios en python PR0402

## Ejercicio 1
```python
scanner = input("Ponga una frase: ").lower().strip()
vocales = 0
consonantes = 0
for letras in str(scanner):
    if(letras in "aeiou"):
        vocales += 1
    else:
        consonantes += 1
print("Vocales encontradas: ",vocales," Consonantes contadas: ",consonantes)
```

## Ejercicio 2
```python
scanner = input("Ponga una cadena: ")
print(str(scanner)[::-1])
```

## Ejercicio 3
```python
scanner = input("Escribe una palabara para sabner si es palindroma: ")
if(str(scanner)[::-1] == str(scanner)):
    print("La palabra ",scanner, " es palindroma")
else:
    print("La palabra ",scanner," es palindroma")
```

## Ejercicio 4
```python
scanner = input("Pon una frase: ")
palabras = str(scanner).split()
print(len(palabras))
```

## Ejercicio 5
```python
scanner = input("Pon palabra: ").lower().strip()
caracteres = ""
for carac in scanner:
    if carac not in caracteres:
        caracteres += carac
print(caracteres)
```

## Ejercicio 6
```python
scanner = input("Pon una cadena: ")
mayusOminus = input("Lo quieres en mayusculas o minusculas: ").lower()

if(str(mayusOminus) == "mayusculas"):
    print(str(scanner).upper())

if(str(mayusOminus) == "minusculas"):
    print(str(scanner).lower())
```

## Ejercicio 7
```python
scanner = input("Pon una cadena: ")
numeroRo = input("Cuantas veces lo rotaras: ")
print(str(scanner)[int(numeroRo):]+str(scanner)[:int(numeroRo)])
```

## Ejercicio 8
```python
scanner = input("Pon una palabra: ").lower()
scanner2 = input("Pon otra palabra: ").lower()
#Sorted lo que hace es coge la cadena sin mas y la ordena plan lo pone por orden osea si pones:
# hola = {'a','h','o','l'} entonces si lo que sale en esta lista es igual a la otra sera anagrama
if sorted(scanner2) == sorted(scanner):
    print("anagrama")
else:
    print("no anagrama")
```

## Ejercicio 8.1
```python
scanner = str(input("Pon una palabra: "))
scanner2 = str(input("Pon otra palabra: "))
anagrama = True

if(len(scanner) == len(scanner2)):
    for c in scanner:
        if not c in scanner2:
            anagrama = False
    for c in scanner:
        if not c in scanner:
            anagrama = False
else:
    anagrama = False

print("ES anagrama" if anagrama else "No lo es")
```

## Ejercicio 9
```python
scanner = str(input("Pon una palabra: "))
#uso un diccionario para poder coger y contar los caracteres, es decir que añado el caracter si  #no ta y si esta solo sumo para saber cuantos hay
frecuencia = {}
for carac in scanner:
    if carac in frecuencia:
        frecuencia[carac] += 1
    else:
        frecuencia[carac] = 1
print(frecuencia)

```

## Ejercicio 10
```python
scanner = str(input("Pon una palabra: "))
caracteres = ""
for carac in scanner:
    if carac.isalnum():
        caracteres += carac
print(caracteres)
```

## Ejercicio 11
```python
scanner = str(input("Pon una frase: "))
scanner = scanner.title().replace(" ","").replace("-","")
scanner = scanner[0].lower()+scanner[1:]
print(scanner)
```

## Ejercicio 12
```python
scanner = str(input("Pon una palabra: "))
prev = ""
cont = 0
res= ""
for i in scanner:
    if prev == i:
        cont += 1
    else:
        if prev:
            res = res + prev
            res = res + str(cont)
        prev = i
        cont = 1
res = res + prev
res = res + str(cont)
print(res)
```

## Ejercicio 19
```python
from random import *
scanner = int(input("Longitud: "))
psw = ""
for i in range(scanner):
    psw += chr(randint(33,255))
print(psw)
```

## Ejercicio 19.1
```python
from random import *
lon = int(input("Dime la longitud: "))
caracteres = "abcdefghijklmnñopqrstuvwxyzABCDEFGHIJKLMNÑOPQRSTUVWXYZ0123456789.,-_:;@#$"
psw = ""
for i in range(lon):
    psw += caracteres[randint(0,len(caracteres)-1)]
print(psw)
```