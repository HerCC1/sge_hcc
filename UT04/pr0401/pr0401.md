# Ejercicios en python PR0401

## Ejercicio 1

```python
num = input("Introduzca un numero: ")
while (not num.isdigit()):
    num = input("eso no era un numero. Pon un numero: ")
else:
    print("numero puesto: " ,num)
```

---

## Ejercicio 2
```python
numTbl = input("Pasa en numero que quieres para la tabla: ")
cuantos = input("Hasta que numero quieres que multiplique: ")

for numer in range(1,int(cuantos)+1):
    print(numTbl, "*", numer, "=", int(numTbl)*int(numer))

```

## Ejercicio 3

---

```python
scanner = input("introduzca el numero: ")
for numero in range(1, int(scanner)+1,1):
    print("*"*numero)
```

## Ejercicio 4

---

```python
scanner = input("introduzca el numero: ")
while (int(scanner) %2 == 0):
    scanner = input("Introduzca un numero impar: ")

for numero in range(1, int(scanner)+1,2):
    espacio = ((int(scanner) - numero) // 2)
    print(" "*espacio,"*" * numero) 
```

---

## Ejercicio 5

```python
numeros = []
for listaNumeros in range(5):
    num= int(input("introduce el numero: "))
    numeros.append(num)

numMayor = max(numeros)
numMenor = min(numeros)
print("El numero mas grande es: ",numMayor, " y el menor es: ",numMenor)
```

---

## Ejercicio 6

```python
converios = {"mm": 0.001,
             "cm": 0.01,
             "m": 1,
             "km": 1000}

valor = int(input("Numero a converitr: "))
valorDe = input("Pasar de(mm, cm, m, km): ")
valorA = input("Pasar a (mm, cm, m, km): ")

if valorDe in converios and valorA in converios:
    numero = valor * converios[valorDe]
    resultaod = numero / converios[valorA]
    print(valor, valorDe, "= ", resultaod, valorA)
else:
    print("converision no valida ")

```

---

## Ejercicio 7

```python
from random import randint

secreto = randint(0,100)
intentos = 0

while True:
    intento = int(input("di un numero del 0 al 100: "))
    intentos += 1

    if (intento == secreto):
        print("Correcto (que suerte)")
        break
    elif (intento < secreto):
        print("el numero es mayor")
    else:
        print("el numero es menor")
```

---

## Ejercicio 8

```python
from random import choice
opciones = ["piedra",
            "papel",
            "tijeras",
            "lagarto",
            "spock"]

ganador = {"piedra": ["lagarto", "tijeras"],
           "papel": ["piedra", "spock"],
           "tijeras": ["papel", "lagarto"],
           "lagarto": ["papel", "spock"],
           "spock": ["piedra","tijeras"]}

ptoJKugador = 0
ptoMaquina = 0

while (ptoJKugador < 5 and ptoMaquina < 5):
    jugador = input("Piedra, papel, tijeras, lagarto, spock: ")
    maquina = choice(opciones)

    if ( jugador == maquina):
        print("empate")
    elif (maquina in ganador[jugador]):
        print("ganas la ronda")
        ptoJKugador += 1
    elif (jugador in ganador[maquina]):
        print("perdisstes la ronda")
        ptoMaquina += 1
    print("Tu: ",ptoJKugador, " Maquina: ", ptoMaquina)

if(ptoJKugador == 5):
    print("Gg ganates")
else:
    print("perdistes")
```
