# Ejercicios en python PR0404

## Ejercicio 1
```python
scanner = str(input("Introduzca el producto:"))
prodcutos = {
    "manzana": 0.95,
    "pera": 0.50,
    "melocoton": 1,
    "sandia":2
}
if (scanner in prodcutos):
    print(scanner, ": ",prodcutos.get(scanner))
else:
    print("Eso no existe")
```

## Ejercicio 2
```python
productos = {
    "Electrónica": ["Smartphone", "Laptop", "Tablet", "Auriculares", "Smartwatch"],
    "Hogar": ["Aspiradora", "Microondas", "Lámpara", "Sofá", "Cafetera"],
    "Ropa": ["Camisa", "Pantalones", "Chaqueta", "Zapatos", "Bufanda"],
    "Deportes": ["Pelota de fútbol", "Raqueta de tenis", "Bicicleta", "Pesas", "Cuerda de saltar"],
    "Juguetes": ["Muñeca", "Bloques de construcción", "Peluche", "Rompecabezas", "Coche de juguete"],
}
nmCat = len(productos)
print("hay ",nmCat, " categorias ")
nmPro = 0

for cat, produc in productos.items():
    numPr = len(produc)
    nmPro += numPr
    print(cat, " tiene :", numPr, " productos")

print("hay ",nmPro," de prdictos")
```

## Ejercicio 3
```python
scanner = input("Pon una frase: ")
frec = {}
for frecuen in scanner:
    if frecuen in frec:
        frec[frecuen] +=1
    else:
        frec[frecuen] = 1
print(frec)
```

## Ejercicio 4 
```python
asignaturas = {
    "Matemáticas": ["Ana", "Carlos", "Luis", "María", "Jorge"],
    "Física": ["Elena", "Luis", "Juan", "Sofía"],
    "Programación": ["Ana", "Carlos", "Sofía", "Jorge", "Pedro"],
    "Historia": ["María", "Juan", "Elena", "Ana"],
    "Inglés": ["Carlos", "Sofía", "Jorge", "María"],
}

print("1: Listar estudiantes matriculados en una asignatura")
print("2: Matricular un estudiante en una asignatura")
print("3: Dar de baja un estudiante de una asignatura.")

scanner=input("Opcion: ")
if scanner == "1":
    for asigntra in asignaturas:
        print(asigntra)
    matricular = input("Elige la asignatura: ")

    if  matricular in asignaturas:
        for estudiante in asignaturas[matricular]:
            print("Estudiantes: ",estudiante)
    else:
        print("Asignatura no encontrada")

if scanner == "2":
    asigntra = input("Asignatura: ")
    estudiante = input("Nombre: ")

    if asigntra in asignaturas:
        if estudiante in asignaturas[asigntra]:
            print("Este estudiante ya esta")
        else: 
            print(estudiante," matriculado en ",asigntra)
    else:
        print("No ta la asignatura")

if scanner == "3":
    asigntra = input("Asignatura: ")
    estudiante = input("Nombre: ")
    if asigntra in asignaturas:
        if estudiante in asignaturas[asigntra]:
            print(estudiante," dadod de baja de ",asigntra)
        else:
            print(estudiante," no esta")
    else:
        print("no esta la asignatura")

```

## Ejercicio 6
```python
prodcutos = {
    "pera":2.00,
    "manzana":1.50,
    "platano":1.00
}

prodcutos2 ={
    "naranja":2.20,
    "manzana":0.50,
    "sandia":5.00
}   

prodcutosCp = prodcutos.copy()

for productos, precio in prodcutos2.items():
    if productos in prodcutosCp:
        prodcutosCp[productos] += precio
    else :
        prodcutosCp[productos] = precio

print(prodcutosCp)
```

## Ejercicio 7
```python
empleados = {
    "Paco":500,
    "Pepe":700,
    "Maria":800,
    "Ana":900
}

scanner = int(input("Filtrado de salario: "))

for empleado, salario in empleados.items():
    if salario > scanner:
        print(empleado, salario)
```